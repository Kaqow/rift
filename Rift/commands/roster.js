const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {


//Roster DM command (still need to make it)
message.channel.send("loool");

};

module.exports.help = {
  name: "roster"
}
